name: Release
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        platform: [ubuntu-22.04, windows-latest]
    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libssl-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install frontend dependencies
        run: npm install

      - name: Build and Upload Assets
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'App v__VERSION__'
          body: 'See the assets to download this version and install.'
          draft: false
          prerelease: false

  # release-arch:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: write
  #   container:
  #     image: archlinux:base-devel
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
        - name: Install System Dependencies
          run: |
            pacman -Syu --noconfirm
            pacman -S --noconfirm pkgconf webkit2gtk libsoup3 openssl gtk3 libappindicator-gtk3 librsvg nodejs npm rustup git wget file
            echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
            locale-gen
            export LANG=en_US.UTF-8  #
  #     - name: Verify Library Existence (Debug)
  #       run: |
  #         ls -la /usr/lib/pkgconfig | grep javascript || echo "Not found in /usr/lib/pkgconfig"
  #         pkg-config --list-all | grep javascript || echo "pkg-config cannot see javascriptcore"
  #
  #     - name: Setup Rust
  #       run: |
  #         rustup default stable
  #
  #     - name: Install Frontend Dependencies
  #       run: npm install
  #
  #     - name: Build Arch Package
  #       env:
  #         PKG_CONFIG_PATH: /usr/lib/pkgconfig:/usr/share/pkgconfig
  #       run: npm run tauri build
  #
  #     - name: Upload Arch Asset
  #       uses: softprops/action-gh-release@v1
  #       if: startsWith(github.ref, 'refs/tags/')
  #       with:
  #         files: src-tauri/target/release/bundle/pacman/*.pkg.tar.zst
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}