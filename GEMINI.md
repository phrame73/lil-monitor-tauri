# 项目说明

- **项目类型**：Tauri + React + TypeScript
- **后端路径**：`src-tauri`
- **代码风格**：
    - **Rust (后端)**: 使用 `snake_case`
    - **TypeScript/React (前端)**: 使用 `camelCase`

> **注意**：每次修改 Rust 代码后，请提醒我运行 `cargo check` 以确保后端逻辑正确。
